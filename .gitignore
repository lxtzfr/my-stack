node_modules/
dist/
.idea/
# Generated by ci-scripts' own `prepare` script (scripts/write-commit.mjs) at install time —
# committing this would freeze a stale commit hash instead of reflecting whatever's actually
# pushed. See shared/update-check.mjs for why it exists.
ci-scripts/.commit.json
